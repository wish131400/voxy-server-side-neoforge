# 更新日志

## 0.3.3（相对 0.3.2，包含后续优化）

更新日期：2026-09-22。适用 Minecraft 1.21.1 / NeoForge、Minecraft 1.20.1 / Forge。

- **减少内存占用**：预测网格新增无损压缩，优先使用可用的 Zstd，没有时使用 Java LZ4；Forge 已内置 LZ4，无需额外安装。当前实机场景中，网格数组连同上传暂存从约 1502 MiB 降至 331 MiB，节省约 1.14 GiB；这是该批网格的对照，不是整个游戏的内存降幅。
- **加快缓存恢复**：预测缓存改用带索引的区域文件，减少小文件访问；新增成品网格缓存，命中后跳过重复构网和接缝构建，让已有精细 LOD 更快恢复。旧缓存按需读取、迁移，无需手动清理；首次使用会逐步补齐成品记录。
- **降低渲染开销**：复用已完成使用的 GPU 缓冲，减少网格更新时的资源分配与驱动开销；支持时使用共享几何缓冲，并在合适的无光影场景批量提交地块。光影与不支持的环境保留兼容路径，不降低地形采样或网格精度。
- **改善远处闪烁**：新增默认开启的“远景抗锯齿”开关，改善纹理过滤，减轻远处雪坡、树冠的细线闪烁；开启光影时由光影处理屏幕抗锯齿。
- **修复雪地显示**：修正覆雪草方块侧面与接缝错误染绿的问题，改善树冠覆雪；合并连续薄雪面，减少多余网格，保留雪层高度。
- **改善冰水交接**：调整预测 LOD 与 Voxy 透明地形的绘制交接，减少冰面、水面重叠显示问题。
- **暂停时降低 GPU 负担**：预测开启且单人世界真正暂停时，默认将帧率上限降至 30 FPS，继续游戏后解除；可通过 `predictionPausedFps` 调整，设为 0 关闭。多人游戏普通菜单不受影响。
- **完善 NeoForge 兼容**：合入 PR #5，修复通过 Roxy 加载 Voxy 时的网格注入崩溃，保留普通 Voxy 支持。此项不适用于 Forge。

网格压缩默认开启，可用 `predictionCompressMeshes` 关闭后重进世界对照。压缩恢复在后台进行；成品缓存仍需输入检查和 GPU 上传，不承诺整个世界瞬间加载。具体收益取决于场景、缓存命中率及硬件。

版本仍为 0.3.3，网络协议与 0.3.2 保持一致。JAR 继续输出至 `lib`，保留原文件名与加载器、Minecraft 版本后缀。

实现与测试记录：[区域缓存](docs/prediction-region-storage.md)、[成品网格缓存与批量提交](docs/finished-mesh-and-indirect-implementation.md)、[雪地与暂停降耗](docs/snow-pause-memory-review.md)、[CPU 网格压缩实测](docs/cpu-mesh-compression.md)。

## 0.3.2（相对 0.3.1）

适用版本：Minecraft 1.21.1 / NeoForge、Minecraft 1.20.1 / Forge。更新日期：2026-09-21。

**本次完善下界预测与跨视距交接，修复大型世界生成档案导致的登录断开，并优化精细缓存恢复、望远镜任务和预测渲染主线程。**

### 联机与世界生成档案

- 修复 NeoForge 将完整世界生成档案放在一个数据包中，超过 Minecraft 单包限制后导致玩家登录被踢的问题。远程同步改为每片最多 512 KiB，客户端完整重组、校验后再安装档案。
- 两个加载器统一有界发送与重组生命周期：处理不完整传输、异常长度、传输替换、断线清理和超时，编码失败时不发送半份档案。单机房主保留进程内直接交付。
- NeoForge 网络协议由 **46 升至 47**，服务端和所有 VSS 客户端必须一起更新。Forge 保持原有分片线格式及协议 **46**。

### 下界、末地与地表内容

- 下界改用紧凑的多层实体柱预测，保留洞穴地板、顶板、空气间隙与熔岩，解决只采到基岩顶层、下方没有可用预测地形的问题。采样、网格、接缝、深度范围和磁盘缓存共同使用多层数据。
- Rust 下界地表规则覆盖各层洞穴地面和顶板；不受支持的原生上下文或规则保留 Java 兼容路径。
- 增加受支持下界装饰的 Rust 放置，包括巨型菌、菌类植被、荧石、垂泪藤／缠怨藤、泉液及相关放置条件。未知模组 codec 和尚未支持的放置器继续走 Java 回退。
- 下界建筑预测接入共享的虚拟区块与编辑事务；要塞、堡垒遗迹使用 Minecraft 的 Java 布局和模板处理器，可与 Rust 或 Java 地形后端配合。**并非将任意模组建筑算法全部重写为 Rust。**
- 保留下界装饰和结构的挖空、替换、液体与悬挂面，只细分实际受编辑影响的粗网格及相邻格，避免房间挖空误删旁边岩石。远处增加基于已有采样的简化巨型菌表示。
- 末地精细半径扩大到普通配置的 **1.5 倍**，上限为预测总距离与 4096 方块中的较小值；保持真实虚空，不填补岛屿间本来就存在的空洞，不增加工作线程数。
- 修复预测火焰绿色、实心及形状异常的问题。普通火和灵魂火采用共享的静态资源包纹理与透明轮廓，保持直立双面表示；每团火不运行动画或粒子模拟。

### 地形交界与渲染修复

- 真实覆盖判定结合当前 Voxy／原版实际渲染距离，避免远处虽然有 Voxy 缓存、但当前视距内没有真实画面时，预测地形仍被错误隐藏。
- 读取 Voxy 自身的深度裁剪范围和反向深度约定，修正远处借用深度重建、预测与真实地形交接及相关水面判断。
- 按当前邻居和实际归属替换过时的地块边缘墙，减少拉长墙遮挡地形细节；邻居退出时恢复必要的边缘封闭，保留装饰／结构表面及已确认洞穴。
- GPU 纹理上传隔离外部像素解包缓冲、行跨度、跳过量和字节序，并在结束或异常时恢复，修复与其他渲染器共享 OpenGL 状态时的一类错误上传风险。该修复不等于已确认解决所有驱动崩溃反馈。

### 缓存、内存与望远镜

- 缩小预测距离后及时退役新范围外且不再需要的 CPU／GPU 地块，保留仍适用的近处数据；GPU 删除继续按预算分帧处理，磁盘缓存保留供后续恢复。
- 压缩缓存读写加入缓冲，减少逐整数／字节调用压缩流的开销；已规范化的植被缓存复用整理结果，降低重进世界时重复处理树叶与竹子的成本。
- 当前规划需要的精细地形缓存增加独立、有界的恢复通道：后台分批探测文件头，命中后可绕过普通逐级生成等待直接恢复已保存的精度，同时保留内存及上传预算。
- 损坏、过期或身份不符的缓存退出恢复通道，由正常生成流程补缺；恢复通道不会在缓存读取失败后直接启动昂贵采样。缓存仍需解压、构网和 GPU 上传，不承诺整个世界秒加载。
- 望远镜将较大范围的地形优先级与准星中央的完整装饰分开：完整装饰集中在 256 方块半径，地形优先级范围保留至 1024 方块；限制昂贵任务排队数量、自动补位，并稳定轻微准星抖动，减少一次开镜集中抢占 CPU。

### 渲染主线程优化

- 地形接缝使用已上传的驻留地块建立邻接关系，视锥仅筛选最终绘制对象，原地转动不再因邻居进出屏幕而重建接缝。
- 保留未变化的接缝网格、包围盒、边界结果和绘制对象；空间索引只定位局部变化附近的拥有者，真实区块交接边界也采用局部更新。
- 持久化渲染场景与可见绘制计划，稳定帧复用集合和顺序；局部上传仅更新相关成员，减少逐帧临时集合和重复查找。
- GPU 驻留快照改为不可变分段表，局部上传只复制受影响的页，旧帧仍保持一致的地块／归属视图。
- 单项验证中，1024 个同级地块修改一个只更新 9 个附近接缝拥有者，网格与全量重建一致；8192 地块的局部快照发布中位耗时在一次 NeoForge 测试中由 1.936 ms 降至 0.025 ms。**这些是组件结果，不是整机 FPS 提升倍数。**

### 诊断、验证与排查结果

- 新增 `/vssclient prediction rendercapture`，手动采集异常帧附近的渲染目标、深度、纹理及光影状态，用于定位角色透明等兼容问题；空闲时不持续进行 GPU 回读。
- Forge 的 `VSS LOD wire` 诊断日志受 `debugLogging` 控制。
- 补充网络分片、磁盘缓存、下界体积与装饰、结构、接缝、驻留生命周期和真实 OpenGL 回归。跨地块合并提交完成可运行原型评估，**本版本未启用正式跨地块合批**。
- 发布验证：NeoForge 全量构建测试 **895 项通过、30 项条件跳过**，Forge **901 项通过、28 项条件跳过**，均无失败；另行启用的 GPU 回归两个加载器各 **3 项通过**。Rust 测试 **114 项通过、3 项基准探针忽略**。条件跳过项依赖额外模组、世界样本或显式测试开关，这些数字不代表已覆盖全部玩家整合包与显卡组合。
- 用户后续确认，此前反馈的角色透明问题不属于 VSS 问题，不再列为本模组的已知缺陷。
- 本次转动视角掉帧已由用户对照确认与鼠标高回报率有关：从 2000／4000／8000 Hz 高回报率档位调整至 **1000 Hz** 后恢复正常。遇到相同现象时可先用 1000 Hz 对照；本项是输入设置排查结果，不计作 VSS 代码修复。

### 升级说明

- 对应产物为 `lib/vss-0.3.2-neoforge-1.21.1.jar` 和 `lib/vss-0.3.2-forge-1.20.1.jar`；退出游戏后替换对应旧 JAR 并重启。
- NeoForge 客户端与服务端同时升级。无需删除世界或 Voxy 缓存；旧地形缓存继续兼容读取，下界旧算法数据自动失效，旧版装饰缓存按新格式规则读取／更新，部分地表内容会重新生成。
- 本次包括随包 Rust 原生库更新。Windows JNI 在本机验证；其他平台原生库已交叉构建，不代表已完成对应操作系统和全部整合包的实机验收。

实现与验证范围详见 [档案分片](docs/worldgen-profile-fragmentation.md)、[下界与末地](docs/nether-end-prediction.md)、[静态火焰与缓存恢复](docs/prediction-static-fire-cache-restore.md)、[增量渲染](docs/prediction-incremental-rendering.md) 和 [转视角实机记录](docs/prediction-camera-turn-followup.md)。

## 0.3.1（相对 0.3）

适用版本：Minecraft 1.21.1 / NeoForge、Minecraft 1.20.1 / Forge。更新日期：2026-09-20。

**本次集中优化预测 LOD 的持续渲染和移动细化开销，并修复光影实体闪烁及慢帧下的望远镜黑屏问题。**

### 修复

- 修复预测渲染在 Voxy / Iris 回调内污染 Minecraft 缓存状态的问题，避免后续实体纹理、深度和混合状态切换被错误跳过。这是开启光影后女仆、骷髅马等模型闪烁的一项已复现原因；保留异常退出时的状态恢复，并适配 Forge / Oculus 路径。
- 修复慢帧下望远镜缩放动画越过目标、反向振荡，导致外围黑色遮罩盖满画面的问题。正常帧保留原动画，慢帧限制插值范围；同时覆盖原版 HUD 和 Forge 的独立望远镜层。
- 修复望远镜精细缓存增多后，预测快照构建反复执行全量地形配对扫描的问题，降低后续移动、细化和 GPU 上传时的主线程停顿。

### 渲染性能优化

- 光影不透明与透明通道在同帧、同视图下复用绘制计划，减少第二次地形扫描、排序、归属遮罩和接缝准备；相机、世界或资源变化时正确失效。
- 渲染入口直接判断是否存在就绪地形，取消仅为判空而复制、排序完整地形列表的操作。
- 已发布且未变化的归属遮罩直接复用；同尺寸 GPU 遮罩更新使用 R8 存储和原位更新，减少数组比较、重复分配与纹理重建。
- 复用未变化的接缝输入和绘制批次，绘制顺序变化不再触发无关重建；局部地形变化只检查可能受影响的邻接区域。
- 将望远镜归属族判定改为四叉树祖先查找，计算量从“精细地形数 × 全部地形数”降至与地形数量和 LOD 层数相关；保留精细缓存、负坐标、维度隔离及原有父子交接规则。
- 减少片元阶段重复的材质元数据查询，提前拒绝不属于当前网格的像素；缓存着色器 uniform、稳定材质映射及纹理布局，复用矩阵上传数组。
- 将接缝和归属遮罩的上传成本计入可选细化上传预算，完整保留必须同时生效的交接更新。
- 按实际地形数量预分配快照索引，减少扩容；新增可选 CPU / GPU 分阶段诊断，GPU 查询异步读取。

### 测试与性能口径

- 在相同的 8192 块缓存地形、1638 块望远镜精细地形测试中，归属族构建中位耗时：NeoForge **103.51 → 1.56 ms**，Forge **107.61 → 1.92 ms**；新旧算法输出一致。该数据仅代表这一计算步骤，不是整帧耗时或游戏 FPS 提升倍数。
- 已验证帧计划与缓存失效、父子上传交接、望远镜切换、接缝输出一致性，以及普通 / Iris 的洞口、岩壁、植被、水面和实体状态 GPU 回归。最后一轮专项验证为两个加载器各 **23 个用例通过**，不是整个版本测试总数。
- GPU 回归在本机 NVIDIA 隐藏 OpenGL 上下文运行；未完成所有玩家光影包组合及 AMD 实机验收，不承诺统一帧率提升幅度。详见 [渲染性能记录](docs/prediction-render-performance.md)、[后续优化记录](docs/prediction-render-followup.md) 和 [黑屏与移动卡顿诊断](docs/black-flash-and-mfg-review.md)。

### 升级说明

- 版本号更新为 `0.3.1-neoforge-1.21.1` / `0.3.1-forge-1.20.1`，对应 JAR 输出到仓库根目录 `lib/`。
- 退出游戏后替换对应 JAR 并重启；从 0.3 升级不需要手动清理预测缓存或 Voxy 缓存。本次未改动网络协议及缓存格式。
- 本次性能优化保持地形采样精度、后台线程配置和现有近处优先交接规则，不以调整帧率熔断代替计算与渲染优化。
- NVIDIA 多帧生成仅完成可行性调查，**未作为 0.3.1 功能加入**。

## 0.3（相对 0.2.12）

适用版本：Minecraft 1.21.1 / NeoForge、Minecraft 1.20.1 / Forge。整理日期：2026-09-18。

**本次主要更新是新增客户端预测 LOD 系统。** 在 0.2.12 的服务端真实区块读取、生成与 LOD 传输基础上，0.3 可以根据服务端同步的世界生成信息，在客户端提前绘制远处地形，再与实际加载的区块和 Voxy 真实 LOD 交接。

### 新增：客户端预测 LOD

- 新增基于世界种子与维度生成信息的远景预测，在远处真实区块尚未生成或传输完成时提前显示地形。
- 新增服务端世界生成快照同步，包括预测所需的动态注册表、生成器信息、生物群系及结构模板等内容；客户端按维度重建预测上下文。
- 新增独立预测渲染层，显示地表、外露岩壁、水面、雪冰及生物群系颜色，并结合原版区块和 Voxy 深度进行遮挡与交接。
- 新增由粗到细的渐进加载、近处与当前视野优先调度、望远镜目标细化及空闲时继续细化。
- 新增地表植被与受支持结构的预测，包括近处及望远镜区域的树木、草和地表建筑，以及远处简化树木与草。
- 新增预测任务、内存驻留及 GPU 上传预算，过期任务提前退出，控制后台工作对游戏帧时间的影响。
- 新增运行时开关与维度生命周期处理，支持进入世界后再开启预测、切换维度后恢复预测。
- 新增预测状态与采集诊断，可通过 `/vssclient stats` 查看状态，使用 `/vssclient prediction capture` 导出参考数据。

### 新增：Rust 原生采样核心与 Java 兼容路径

- 新增随模组打包、从源码构建的 Rust 世界生成核心，通过 JNI 为受支持生成器提供噪声、密度、地表和生物群系相关采样。
- 提供粗略预览、显示地形和精确查询等分阶段路径：先建立远景覆盖，再补充材质、液体和地表内容；不满足快速路径条件的情况保留更完整的采样回退。
- 增加批量采样、密度工作区复用、颜色与生物群系缓存、植被地面查询复用，减少重复计算和分配。
- 新增 Java 预测后端，在原生库不可用或生成图不受支持时，对受支持的已解码上下文使用 Java 采样。共享的调度、缓存及渲染优化仍适用，但不代表 Java 获得 Rust 内部优化的同等速度。
- 随包提供 Windows x64、Linux x64 / ARM64、macOS x64 / ARM64 原生库；原生库平台支持与 Voxy 的图形平台支持是不同条件。

### 新增：预测地形持久化

- 新增客户端本地压缩预测缓存，保存多级地形采样和地表内容，重进世界可恢复已有精度，减少重复地形采样。
- 按世界生成快照区分缓存，稳定生成快照标识，避免仅因对象字段顺序变化而不必要地重建缓存。
- 保存生物群系颜色，并根据草、叶颜色表指纹识别资源包变化；颜色表变化时可以复用地形并更新颜色。
- 预测缓存与 Voxy 真实 LOD 存储分工独立。预测缓存命中后仍需解压、构建渲染网格和上传 GPU，并非立即恢复所有画面。

### 新增：预测设置与兼容适配

- 客户端预测默认开启，可通过 `enablePrediction` 控制；服务端可通过 `enablePredictionSync` 控制世界种子及生成元数据的同步。
- 新增独立预测距离、精细距离、地表内容距离、细节档位、后台任务额度、植被、结构、雾及缓存开关；相关设置接入 Voxy / Sodium / Embeddium 设置页。
- 默认预测距离为 **4096 方块**，普通精细距离为 **512 方块**，地表细化宽度为 **768 方块**，`rememberTerrain` 默认开启。已有用户配置保留。
- 最终版本不采用最外侧 10% 强制粗 LOD 的限制；实际细化仍受屏幕像素大小、视野、内存和任务预算约束。
- 增加普通渲染与 Iris 光影路径的预测合成、深度交接和图形能力检测。不满足预测渲染条件时停用预测层。
- 增加 TerraBlender 区域、Blueprint 切片及嵌套生物群系路由的处理，修正区域选择丢失引起的地表材质差异。
- 增加 Tectonic、FreeTerraForged、史诗地形等生成路径的专门适配及验证工具，并处理 Java 密度缓存与 C2ME 的兼容性。具体游戏版本与适配范围以各仓库说明为准，不代表任意模组版本或整合包组合均兼容。

### 改进：真实 LOD 请求、预测交接与算力分配

- 在既有近处优先请求基础上，完善真实列就绪跟踪，结合数据接收、Voxy 入库、网格处理和上传状态推进后续请求。
- 已有 Voxy 远处缓存继续按 Voxy 自身的视距与可见性规则显示；新增请求的近到远顺序不用于整体隐藏已有远处缓存。
- 根据真实列覆盖与实际渲染深度进行局部交接，减少预测遮挡真实地形、交界处突然缺失大片地形，以及高空视角下的覆盖错误；没有真实深度时保留预测兜底。
- 完善不同精度网格之间的接缝、已确认洞穴内部侧面、岩壁占用和水面处理，减少缺面及长墙遮挡。
- 开启预测时优先建立初始预测覆盖，再分阶段恢复新预生成请求并发；关闭预测时恢复正常分配。保留已有在途工作、缓存查询、传输和脏列刷新。
- 增加停滞与超时恢复，避免预测没有进展时长期阻塞新预生成；多人游戏中调节的是本客户端请求需求，不是独占服务端 CPU。
- 限制覆盖扫描、索引刷新和延后队列的重复工作，增加重试退避，减少预算耗尽时的无效遍历。
- 修复异步区块请求复用可变数组，避免后续扫描改变尚未编码的请求。
- 改进 NBT 列完整性判断及额外光照节处理，减少将可用列误判为不完整所造成的重复读取或生成。

### 修复：真实 LOD 生物群系数据

- 修复区块快照序列化时丢失生物群系调色板的问题，保留对应生物群系数据，避免真实 LOD 的草、树叶、水等着色异常。
- 更新持久化列缓存格式标记。升级后旧格式缓存会自动视为无效并重新读取或生成，无需手动删除；首次恢复相关真实 LOD 可能需要额外工作。

### 性能、内存与开发工具

- 为预测任务引入分阶段初始化、中等精度覆盖优先、受预算约束的细化及工作区复用。
- 使用分页有效覆盖索引与紧凑扫描偏移，限制活动范围并将偏移构建、排序移出 tick 线程。
- 复用未变化的渲染归属结果，共享相同的不可变采样和高度数组，减少对象及重复数据驻留。
- 记忆确定性的网格容量失败，避免输入未变时反复采样与构建；数据或设置变化后允许重试。
- 补充 Java / Rust、CPU / GPU 回归测试，以及密度边界、材质、液体、完整显示结果、回退次数和高度／水面差异的对照工具。
- 新增原生库跨平台构建和包审计工具，检查架构、JNI 导出、依赖与包内哈希。
- 两个加载器版本同步主要功能，保留各自网络、渲染和 Minecraft API 适配。构建 JAR 输出到仓库根目录 `lib/`。

### 升级与使用说明

- 客户端和服务端请一起更新到对应游戏版本、同次构建的 0.3；不要与 0.2.12 混用。
- 预测需要服务端允许同步世界种子和生成元数据；希望关闭该同步的服务器可设置 `enablePredictionSync=false`。
- 预测是近似远景，不执行所有完整世界生成步骤，也不能保证复现整合包城市、全部结构、地下洞穴或玩家改动；真实区块及真实 LOD 是最终依据。
- 预测距离较大、复杂地形或密集植被仍会增加 CPU、内存与加载时间；本版本不保证整个 Minecraft 进程低于 10 GiB，也没有适用于所有整合包的统一 FPS 提升比例。
- 预生成恢复依据初始预测覆盖和近处就绪度；极大预测距离下，不等于全部中距离地形已精细化。
- Linux / macOS 原生库已完成交叉构建及包检查，不能据此等同于当地游戏实测；macOS 标准驱动不满足 Voxy 的 OpenGL 4.6 路径要求。

### 版本范围与验证口径

本日志覆盖 **0.2.12 → 当前 0.3** 的累计变化。Git 中没有 0.2.12 标签，以明确将版本设为 0.2.12 的提交为基线：

- NeoForge：[fc806c7 → 0.3 代码快照](https://github.com/wish131400/voxy-server-side-neoforge/compare/fc806c7...6c4bdf4)。
- Forge：[2a234c7 → 0.3 代码快照](https://github.com/wish131400/voxy-server-side-forge/compare/2a234c7...5805a28)。

期间存在内部版本号为 0.2.13 的开发提交，其改动也包含在上述范围内。0.2.12 已有的 Xaero 地图桥接、远处玩家同步、服务端持久化列缓存和基础近处优先请求，不列为 0.3 新增功能。

两个加载器的当前构建及五平台原生包审计通过。最后一次稀疏工作区优化的专项记录为：112 项 Rust 测试通过、3 项既有离线诊断测试忽略，两个加载器各 16 项 Java / 原生相关测试通过；320 次 JVM 对照运行保持完整采样输出与回退次数一致，稀疏场景汇总耗时约下降 3.2%。这些数字仅描述该项后期优化的测试范围，**不是 0.2.12 到 0.3 的整体性能提升或全版本测试总数**。
