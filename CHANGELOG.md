# 更新日志

## 0.3 — 2026-09-18

适用版本：NeoForge / Minecraft 1.21.1、Forge / Minecraft 1.20.1。两条分支共享主要功能，保留各自加载器、网络和渲染接口的适配。

### 预测 LOD 与渐进细化

- 完善预测地形的渐进加载、屏幕空间细节选择、望远镜选区和空闲时细化，让已有粗略地形逐步补充细节。
- 默认预测距离改为 **4096 方块**，精细距离改为 **512 方块**。已有配置保留，不会强制覆盖玩家的设置。
- 取消最外侧 10% 距离强制保持粗 LOD 的限制。实际细化仍受屏幕像素大小、可见性、内存与任务预算约束，不代表所有远处地形都会细化到近处精度。
- 增加远处简化树木和草的显示，配合近处及望远镜区域的更精细植被；修正植被朝向和树叶状态处理。
- 完善预测缓存、维度切换及任务生命周期；修复进入世界时关闭预测、之后再开启不能正常启动的问题。

### 岩壁、洞口与真实地形交接

- 调整岩壁与洞穴网格的占用判断，补充已确认洞穴的内部侧面，减少正常岩石缺面和不合理的长墙。
- 优化不同精度网格之间的接缝处理，避免用缺乏依据的填充面遮挡地形细节。
- 增加真实列覆盖掩码，结合实际渲染深度处理预测与 Voxy 的交接，减少局部真实地形已经显示时仍被预测岩壁覆盖的问题。
- 保留没有真实深度的区域作为预测兜底，并处理高空、望远镜、透明水面及普通 / Iris 渲染路径的交接。

### 区块请求、缓存与算力调度

- 新增真实 LOD 请求保持近到远推进，并跟踪数据接收、Voxy 入库、网格工作和上传状态。
- **已有 Voxy 缓存按 Voxy 自身的视距与可见性规则正常显示**，不再因近处缺列或请求边界回退而整体隐藏远景。近到远约束用于新增请求，不用于屏蔽已有远处缓存。
- 调整覆盖扫描、索引刷新、重试退避和延后队列处理，限制重复扫描及生成预算用尽后的无效遍历。
- 修复异步区块请求复用可变数组的问题，避免后续扫描改变尚未编码的请求内容。
- 开启预测时，优先完成预测的初始覆盖，再逐步恢复新预生成请求的并发额度；关闭预测后恢复正常分配。已有进行中的工作不会被强制中止，缓存查询、传输及脏列刷新保留原有路径。
- 加入停滞与超时恢复，避免预测长时间没有进展时一直阻塞新预生成请求。多人游戏中，该策略调节本客户端的请求需求，不代表独占服务端 CPU。
- 细化脏列更新、缓存读取及生成开关相关处理。

### 性能与内存

- 优化 Rust 地形显示采样、密度工作区和颜色查询的缓存复用，减少重复分配与计算。
- 最新稀疏采样优化在批量查询中复用工作区，切换区块时清除与原位置相关的缓存，保持采样结果与回退行为一致。
- 真实覆盖索引改为只保存有效覆盖的分页结构，限制活动范围，减少大量无覆盖列的常驻对象。
- 覆盖扫描偏移使用紧凑存储，构建和排序移出 tick 线程；复用未发生变化的渲染归属结果。
- 共享相同的不可变采样数据和高度数组，减少重复内存。
- 记忆确定性的网格容量失败，避免在输入未变时反复采样、构建；数据或设置变化后允许重新尝试。

### 兼容与构建

- 完善 Java 密度缓存与 C2ME 的兼容处理，以及 TerraBlender / Blueprint 等嵌套生物群系路由处理。
- 保留 Rust 不可用时的 Java 兼容路径。调度、网格、覆盖索引和内存等共享优化仍适用；Rust 内部的采样优化不等同于 Java 获得同样的加速。
- 同步 Windows x64、Linux x64 / ARM64、macOS x64 / ARM64 原生库。
- 构建 JAR 统一输出到仓库根目录的 `lib/`，JAR 和本机诊断产物不纳入源码版本控制。
- 补充预测渲染、请求顺序、缓存、内存及原生采样回归测试和可复用诊断工具。

### 验证与已知边界

- 最新原生采样版本：112 项 Rust 测试通过，3 项既有离线诊断测试按条件忽略；两个加载器各 16 项 Java / 原生库相关测试通过。
- 稀疏采样 A/B 使用原版与 Tectonic、两个种子、32 个场景及 5 轮新旧对照，共 320 次 JVM 运行。完整采样输出与回退次数一致；稀疏场景汇总耗时约下降 **3.2%**，不同场景存在波动。这是该项优化的专项结果，不是整个 0.3 的加载时间或 FPS 提升比例。
- 五个平台的原生包通过架构、导出和包内哈希检查；Linux / macOS 为交叉构建与包检查，未据此声称完成当地游戏运行验证。
- 预测地形仍是近似结果，不能保证复现所有整合包的城市、结构或玩家改动；真实区块与真实 LOD 才是最终依据。
- 内存优化不构成整个 Minecraft 进程低于 10 GiB 的硬性保证。
- 预生成恢复门槛依据初始预测覆盖和近处地形就绪度；极大预测距离下，不代表所有肉眼可见的中距离地形已完成精细化。
