package xaero.map.region;

import xaero.map.MapProcessor;

public class MapUpdateFastConfig {
    public final MapProcessor processor;

    public MapUpdateFastConfig(MapProcessor processor) {
        this.processor = processor;
    }
}
